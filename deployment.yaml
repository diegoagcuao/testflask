apiVersion: apps/v1
kind: Deployment
metadata:
  name: testflask-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testflask
  template:
    metadata:
      labels:
        app: testflask
    spec:
      containers:
      - name: testflask
        image: acrtestflask.azurecr.io/testflask:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: testflask-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: testflask
